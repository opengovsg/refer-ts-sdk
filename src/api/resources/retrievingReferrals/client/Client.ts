/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "../../../../environments";
import * as core from "../../../../core";
import * as ReferralExchange from "../../../index";
import urlJoin from "url-join";
import * as errors from "../../../../errors/index";

export declare namespace RetrievingReferrals {
    export interface Options {
        environment?: core.Supplier<environments.ReferralExchangeEnvironment | string>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        apiKey: core.Supplier<string>;
        fetcher?: core.FetchFunction;
    }

    export interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional headers to include in the request. */
        headers?: Record<string, string>;
    }
}

export class RetrievingReferrals {
    constructor(protected readonly _options: RetrievingReferrals.Options) {}

    /**
     * @param {ReferralExchange.InstitutionIdType} institutionIdType - The type of institution ID
     * @param {string} institutionId - The institution ID
     * @param {ReferralExchange.RetrievingReferralsListByInstitutionRequest} request
     * @param {RetrievingReferrals.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link ReferralExchange.UnauthorizedError}
     *
     * @example
     *     await client.retrievingReferrals.listByInstitution("hci", "institutionId")
     */
    public async listByInstitution(
        institutionIdType: ReferralExchange.InstitutionIdType,
        institutionId: string,
        request: ReferralExchange.RetrievingReferralsListByInstitutionRequest = {},
        requestOptions?: RetrievingReferrals.RequestOptions,
    ): Promise<ReferralExchange.PaginatedReferralsDto> {
        const { offset, pageSize, status, role, referrerId, referrerIdType, patientUin } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        if (offset != null) {
            _queryParams["offset"] = offset.toString();
        }

        if (pageSize != null) {
            _queryParams["pageSize"] = pageSize.toString();
        }

        if (status != null) {
            if (Array.isArray(status)) {
                _queryParams["status"] = status.map((item) => item);
            } else {
                _queryParams["status"] = status;
            }
        }

        if (role != null) {
            _queryParams["role"] = role;
        }

        if (referrerId != null) {
            _queryParams["referrerId"] = referrerId;
        }

        if (referrerIdType != null) {
            _queryParams["referrerIdType"] = referrerIdType;
        }

        if (patientUin != null) {
            _queryParams["patientUin"] = patientUin;
        }

        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: urlJoin(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.ReferralExchangeEnvironment.SmartCms,
                `api/v1/institutions/${encodeURIComponent(institutionIdType)}/${encodeURIComponent(institutionId)}/referrals`,
            ),
            method: "GET",
            headers: {
                "X-Fern-Language": "JavaScript",
                "X-Fern-SDK-Name": "@opengovsg/refx-ts-sdk",
                "X-Fern-SDK-Version": "0.0.67",
                "User-Agent": "@opengovsg/refx-ts-sdk/0.0.67",
                "X-Fern-Runtime": core.RUNTIME.type,
                "X-Fern-Runtime-Version": core.RUNTIME.version,
                ...(await this._getCustomAuthorizationHeaders()),
                ...requestOptions?.headers,
            },
            contentType: "application/json",
            queryParameters: _queryParams,
            requestType: "json",
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return _response.body as ReferralExchange.PaginatedReferralsDto;
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 401:
                    throw new ReferralExchange.UnauthorizedError(_response.error.body as unknown);
                default:
                    throw new errors.ReferralExchangeError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.ReferralExchangeError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                });
            case "timeout":
                throw new errors.ReferralExchangeTimeoutError(
                    "Timeout exceeded when calling GET /api/v1/institutions/{institutionIdType}/{institutionId}/referrals.",
                );
            case "unknown":
                throw new errors.ReferralExchangeError({
                    message: _response.error.errorMessage,
                });
        }
    }

    /**
     * @param {string} referralId - Referral ID
     * @param {ReferralExchange.RetrievingReferralsGetRequest} request
     * @param {RetrievingReferrals.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link ReferralExchange.UnauthorizedError}
     * @throws {@link ReferralExchange.NotFoundError}
     *
     * @example
     *     await client.retrievingReferrals.get("referralId", {
     *         requesterIdentifier: "requester@example.com",
     *         institutionIdType: "hci",
     *         institutionId: "institutionId"
     *     })
     */
    public async get(
        referralId: string,
        request: ReferralExchange.RetrievingReferralsGetRequest,
        requestOptions?: RetrievingReferrals.RequestOptions,
    ): Promise<ReferralExchange.FullReferralDto> {
        const { additionalField, requesterIdentifier, institutionIdType, institutionId } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        if (additionalField != null) {
            if (Array.isArray(additionalField)) {
                _queryParams["additionalField"] = additionalField.map((item) => item);
            } else {
                _queryParams["additionalField"] = additionalField;
            }
        }

        _queryParams["requesterIdentifier"] = requesterIdentifier;
        _queryParams["institutionIdType"] = institutionIdType;
        _queryParams["institutionId"] = institutionId;
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: urlJoin(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.ReferralExchangeEnvironment.SmartCms,
                `api/v1/referrals/${encodeURIComponent(referralId)}`,
            ),
            method: "GET",
            headers: {
                "X-Fern-Language": "JavaScript",
                "X-Fern-SDK-Name": "@opengovsg/refx-ts-sdk",
                "X-Fern-SDK-Version": "0.0.67",
                "User-Agent": "@opengovsg/refx-ts-sdk/0.0.67",
                "X-Fern-Runtime": core.RUNTIME.type,
                "X-Fern-Runtime-Version": core.RUNTIME.version,
                ...(await this._getCustomAuthorizationHeaders()),
                ...requestOptions?.headers,
            },
            contentType: "application/json",
            queryParameters: _queryParams,
            requestType: "json",
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return _response.body as ReferralExchange.FullReferralDto;
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 401:
                    throw new ReferralExchange.UnauthorizedError(_response.error.body as unknown);
                case 404:
                    throw new ReferralExchange.NotFoundError(
                        _response.error.body as ReferralExchange.NotFoundErrorBody,
                    );
                default:
                    throw new errors.ReferralExchangeError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.ReferralExchangeError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                });
            case "timeout":
                throw new errors.ReferralExchangeTimeoutError(
                    "Timeout exceeded when calling GET /api/v1/referrals/{referralId}.",
                );
            case "unknown":
                throw new errors.ReferralExchangeError({
                    message: _response.error.errorMessage,
                });
        }
    }

    protected async _getCustomAuthorizationHeaders() {
        const apiKeyValue = await core.Supplier.get(this._options.apiKey);
        return { Authorization: apiKeyValue };
    }
}
